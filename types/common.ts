/**
 * Note:
 *  Generally speaking there's no such need 
 *  for this type-helpers but they make more readable for people with
 *  understanding what kind of data it relates to
 */

export type HTMLText = string // TODO: there's potential use for Zod validation
export type Slug = string
export type DateType = string
export type Uuid = string
